import { TestBed, async } from '@angular/core/testing';
import { GraphPage } from './graph';

/*describe('Component: Graphs', () => {
  it('should create an instance', () => {
    let component = new GraphsComponent();
    expect(component).toBeTruthy();
  });
});*/
